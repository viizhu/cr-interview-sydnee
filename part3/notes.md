* the plan here is to implement a AlertsCard component that has conditional to render whether there are breaches or not. (the alert box should have different messages and colors that pertains to the breach status)
* need to modify the dashboard component to render the alerts card and set up binding to pass in breach related data as input
* the alertscard component recieves data and toggles between the messages
* need to pass potential data from dashboard as input for alert component